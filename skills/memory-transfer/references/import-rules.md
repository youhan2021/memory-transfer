# Import Rules

Supported import modes:

- `append`: add imported memories after existing ones
- `replace`: replace the entire target memory list with imported memories
- `upsert`: update matching `id` items and append new ones

Recommended process:

1. Preview bundle
2. Choose import mode
3. Apply import
4. Review resulting target file
