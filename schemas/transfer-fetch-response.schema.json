{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://memory-transfer.local/schemas/transfer-fetch-response.schema.json",
  "title": "TransferFetchResponse",
  "type": "object",
  "required": ["transfer_id", "expires_at", "consumed", "preview"],
  "properties": {
    "transfer_id": { "type": "string" },
    "expires_at": {
      "type": "string",
      "format": "date-time"
    },
    "consumed": { "type": "boolean" },
    "preview": {
      "type": "object"
    },
    "bundle": {
      "$ref": "./memory-bundle.schema.json"
    }
  },
  "additionalProperties": false
}
